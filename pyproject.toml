[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "government-vehicle-services-rag"
version = "0.1.0"
description = "RAG over MultiDoc2Dial DMV documents"
requires-python = ">=3.11,<3.14"

[tool.setuptools.packages.find]
where = ["src"]

